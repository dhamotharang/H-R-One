using System;
using System.Collections.Generic;
using System.Text;

namespace HROne.DataAccess
{
    //public interface QueryValueConverter
    //{
    //    string ToQueryValue(object obj);
    //}

    //public class MSSqlQueryValueConverter : QueryValueConverter
    //{
    //    public string ToQueryValue(object obj)
    //    {
    //        if (obj is DateTime)
    //            return "CONVERT(datetime, '" + ((DateTime)obj).ToString("yyyy-MM-dd HH:mm:ss") + "', 120)";
    //        else if (obj is string)
    //            return "'" + ((string)obj).Replace("'", "''") + "' ";
    //        else
    //            return obj.ToString();
    //    }
    //}
}
